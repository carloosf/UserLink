generator client {
  provider = "prisma-client-js"
}


datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

//retirar o comentario quando for migrar
//migrationsLocation = "./migrations"


model users {
id Int @id @default(autoincrement())
name String 
email String
senha String 
}
